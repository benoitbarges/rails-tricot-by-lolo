<div class="container">
  <div class="text-center m-4">
    <h1>Add a new product</h1>
  </div>
  <%= simple_form_for(@product) do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :size %>
  <%= f.input :description %>
  <%= f.input :category, label: "Catégorie", collection: Product::CATEGORY %>
  <%= f.input :available, label: "Disponible" %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Edit", class: "btn btn-primary", id:'add-button' %>
  <% end %>
</div>
