<%= render'shared/banner' %>
<div class="container mt-5 mb-5">
  <div class="row">
    <% @products.each do |product| %>
      <div class="col-sm-12 col-md-6 col-lg-4">
          <div class="product-card" style="background-image: url('<%= cl_image_path product.photo.key %>')">

          </div>
      </div>
    <% end %>
  </div>
</div>
