<div class="payment-container">
      <div class="mb-3"><h1>Votre commande a bien été enregistrée.</h1></div>
      <div class="mb-3"><h4>Pour la confirmer, veuillez cliquer sur le lien ci-dessous. Il vous redirigera vers une plate-forme sécurisée de paiements par carte bancaire.</h4></div>
      <button id="pay" class="add-to-cart-btn">Payer</button>

      <script src="https://js.stripe.com/v3/"></script>
      <script>
        const paymentButton = document.getElementById('pay');
        paymentButton.addEventListener('click', () => {
          const stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');
          stripe.redirectToCheckout({
            sessionId: '<%= @order.checkout_session_id %>'
          });
        });
      </script>
</div>
